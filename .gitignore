# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Logs
*.log

# Local build state — which documents are already in this machine's graph
build_ledger.json
# ...and which chunks of a document that crashed mid-build already made it in
chunk_checkpoint.json
conflict_candidates.db

# OS / editor
.DS_Store
.idea/
.vscode/

# host-specific compose config (Tailscale bind) — jangan di-commit
docker-compose.override.yml

# dump/restore lokal — isinya bisa besar, jangan masuk repo
neo4j_backups/
