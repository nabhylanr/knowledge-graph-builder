# VOID — superseded

This document is no longer authoritative. It originated from a miscommunication
with the KG development team and does not describe the intended design.

The authoritative spec for conflict detection, classification, and the
`Contradiction` / `supersedes` output ontology is:

  docs/conflict_pipeline.md

Do not implement against anything in this file's history. It is kept only so
existing references resolve; see git history if you need the original text.